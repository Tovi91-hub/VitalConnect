
VitalConnect.pageInit(() => { const page = window.location.pathname.split('/').pop() || 'index.html'; if (page === 'login.html') initLogin(); if (page === 'register.html') initRegister(); if (page === 'profile.html') initProfile(); if (page === 'dashboard.html') initDashboard(); });
function initLogin(){ const form = document.querySelector('#loginForm'); const feedback = document.querySelector('#formFeedback'); if (!form) return; const demoNotice = document.querySelector('#demoNotice'); if (demoNotice) demoNotice.innerHTML = VitalConnect.createNotice('Demo accounts: demo@vitalconnect.com / Password123! and admin@vitalconnect.com / Admin123!', 'info'); form.addEventListener('submit', event => { event.preventDefault(); const email = form.email.value.trim().toLowerCase(); const password = form.password.value.trim(); const users = VitalConnect.getCollection(VitalConnect.STORE.users); const user = users.find(item => item.email.toLowerCase() === email && item.password === password); if (!user) { feedback.innerHTML = VitalConnect.createNotice('Login failed. Please verify your email and password.', 'error'); return; } VitalConnect.setSession(user); feedback.innerHTML = VitalConnect.createNotice('Login successful. Redirecting to your dashboard.', 'success'); setTimeout(() => window.location.href = 'dashboard.html', 700);});}
function initRegister(){ const form = document.querySelector('#registerForm'); const feedback = document.querySelector('#formFeedback'); if (!form) return; form.addEventListener('submit', event => { event.preventDefault(); const users = VitalConnect.getCollection(VitalConnect.STORE.users); const payload = {id:VitalConnect.nextId(users),name:form.name.value.trim(),email:form.email.value.trim().toLowerCase(),password:form.password.value.trim(),confirmPassword:form.confirmPassword.value.trim(),city:form.city.value.trim(),state:form.state.value.trim(),bio:form.bio.value.trim(),role:'member'}; if (!payload.name || !payload.email || !payload.password || !payload.city || !payload.state) { feedback.innerHTML = VitalConnect.createNotice('Please complete all required fields.', 'warning'); return; } if (!VitalConnect.validateEmail(payload.email)) { feedback.innerHTML = VitalConnect.createNotice('Enter a valid email address.', 'warning'); return; } if (payload.password.length < 8) { feedback.innerHTML = VitalConnect.createNotice('Password must be at least 8 characters long.', 'warning'); return; } if (payload.password !== payload.confirmPassword) { feedback.innerHTML = VitalConnect.createNotice('Passwords do not match.', 'warning'); return; } if (users.some(user => user.email.toLowerCase() === payload.email)) { feedback.innerHTML = VitalConnect.createNotice('An account with this email already exists.', 'error'); return; } delete payload.confirmPassword; users.push(payload); VitalConnect.setCollection(VitalConnect.STORE.users, users); VitalConnect.setSession(payload); feedback.innerHTML = VitalConnect.createNotice('Registration successful. Redirecting to your dashboard.', 'success'); setTimeout(() => window.location.href = 'dashboard.html', 700); });}
function initProfile(){ const session = VitalConnect.getSession(); const users = VitalConnect.getCollection(VitalConnect.STORE.users); const currentUser = users.find(user => user.email === session?.email); if (!currentUser) return; const profileForm = document.querySelector('#profileForm'); const profileFeedback = document.querySelector('#profileFeedback'); profileForm.name.value = currentUser.name || ''; profileForm.email.value = currentUser.email || ''; profileForm.city.value = currentUser.city || ''; profileForm.state.value = currentUser.state || ''; profileForm.bio.value = currentUser.bio || ''; profileForm.addEventListener('submit', event => { event.preventDefault(); currentUser.name = profileForm.name.value.trim(); currentUser.city = profileForm.city.value.trim(); currentUser.state = profileForm.state.value.trim(); currentUser.bio = profileForm.bio.value.trim(); VitalConnect.setCollection(VitalConnect.STORE.users, users.map(user => user.email === currentUser.email ? currentUser : user)); VitalConnect.setSession(currentUser); profileFeedback.innerHTML = VitalConnect.createNotice('Profile updated successfully.', 'success'); });}
function initDashboard(){ const shell = document.querySelector('#dashboardShell'); const session = VitalConnect.getSession(); if (!shell || !session) return; const moods = VitalConnect.getCollection(VitalConnect.STORE.moods).filter(item => item.authorEmail === session.email); const prayers = VitalConnect.getCollection(VitalConnect.STORE.prayers).filter(item => item.authorEmail === session.email); const blessings = VitalConnect.getCollection(VitalConnect.STORE.blessings).filter(item => item.authorEmail === session.email); const help = VitalConnect.getCollection(VitalConnect.STORE.helpRequests).filter(item => item.authorEmail === session.email); shell.innerHTML = `${VitalConnect.dashboardSidebar('dashboard.html')}<section class="grid" aria-label="Dashboard content"><div class="welcome-strip"><div><h2>Welcome, ${VitalConnect.sanitize(session.name)}</h2><p>Track community activity, personal wellness entries, and support requests from one dashboard.</p></div><a class="btn-secondary" href="mood-check.html">Add New Entry</a></div><div class="grid grid-2"><div class="metric-card"><p>Mood Entries</p><strong>${moods.length}</strong></div><div class="metric-card"><p>Prayer Requests</p><strong>${prayers.length}</strong></div><div class="metric-card"><p>Marketplace Posts</p><strong>${blessings.length}</strong></div><div class="metric-card"><p>Help Requests</p><strong>${help.length}</strong></div></div><div class="card"><h3>Quick Actions</h3><div class="badge-row"><a class="btn-ghost" href="prayer-wall.html">Open Prayer Wall</a><a class="btn-ghost" href="blessing-marketplace.html">Share a Blessing</a><a class="btn-ghost" href="help-board.html">Request Help</a><a class="btn-ghost" href="my-posts.html">Manage My Posts</a></div></div><div class="card"><h3>Current Session</h3><p class="mini">Signed in as <strong>${VitalConnect.sanitize(session.email)}</strong> with role <strong>${VitalConnect.sanitize(session.role)}</strong>.</p><p class="mini">VitalConnect is optimized for static hosting and supports secure client-side validation, responsive design, and JSON plus LocalStorage persistence.</p></div></section>`; }
